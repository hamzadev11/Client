import { environment } from 'src/environments/environment'

export const baseUrl = 'http://localhost:3000'
export const prodUrl = baseUrl + '/produit/'
export const cartUrl = baseUrl + '/commande/'
export const panierUrl = baseUrl + '/panier/'
export const livUrl = baseUrl + '/livraison/'
export const factUrl = baseUrl + '/facture/'